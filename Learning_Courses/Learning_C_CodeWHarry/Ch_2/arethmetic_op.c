// Arethmatic operators in the c language

#include <stdio.h>

int main(){
    printf("\n");

    int a = 4;
    int b = 3;
    // addition operator
    printf("The value of a is %d and value of b is %d and sume is %d \n", a,b,a+b);
    // modulus operator
    printf("The reminder when a is divided by b is : %d \n", a%b);

    printf("\n");
    return 0;
}